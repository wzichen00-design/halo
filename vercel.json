{
  "version": 2,
  "builds": [
    {
      "src": "ui/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build/dist/ui",
        "buildCommand": "cd ui && echo '=== Environment Info ===' && node --version && npm --version && echo '=== Installing pnpm ===' && npm install -g pnpm@10.30.3 && pnpm --version && echo '=== Installing dependencies ===' && pnpm install --verbose && echo '=== Building project ===' && pnpm run build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "console.html",
      "status": 200
    }
  ]
}
